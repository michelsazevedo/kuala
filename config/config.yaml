database:
  host: "${POSTGRES_HOST}"
  username: "${POSTGRES_USER}"
  password: "${POSTGRES_PASSWORD}"
  database: "${POSTGRES_DB}"
  timeout: 5

settings:
  secret: Yu3hD78tWq01
  server:
    port: ":8080"